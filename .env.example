# ──────────────────────────────────────────────────────────
#  CJM Builder 환경변수 설정 파일
#
#  사용법:
#    1. 이 파일을 복사해서 '.env' 파일로 저장
#    2. 아래 값들을 실제 값으로 채우기
#    3. .env 파일은 절대 외부에 공유하지 마세요! (API 키 포함)
# ──────────────────────────────────────────────────────────

# [필수] OpenAI API 키
#   - OpenAI 계정 → https://platform.openai.com/api-keys 에서 발급
#   - sk-... 로 시작하는 키
OPENAI_API_KEY=sk-여기에_실제_API키를_입력하세요

# [권장] 사이트 접속 비밀번호
#   - 비워두면 누구나 접근 가능 (사내 네트워크에서만 사용 시 비워도 됨)
#   - ngrok / 클라우드 배포 시 반드시 설정 권장
SITE_PASSWORD=여기에_비밀번호를_입력하세요

# [선택] 세션 서명 키 (랜덤한 긴 문자열)
#   - 비워두면 서버 시작 시 자동 생성 (서버 재시작 시 세션 초기화됨)
#   - 서버를 자주 재시작하는 경우 직접 설정하면 세션 유지됨
SESSION_SECRET=

# [선택] 사용할 GPT 모델 (기본: gpt-4o)
#   - gpt-4o : 최고 품질 (권장)
#   - gpt-4o-mini : 더 빠르고 저렴
OPENAI_MODEL=gpt-4o
